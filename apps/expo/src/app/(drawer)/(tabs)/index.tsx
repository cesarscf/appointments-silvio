import { useRouter } from "expo-router";
import { SafeAreaView } from "react-native";

export default function AppointmentsScreen() {
  const router = useRouter();

  return <SafeAreaView className="flex-1 bg-gray-50"></SafeAreaView>;
}
